<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mangalog.ryuuga.a40kdiceapp">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SettingsMenu"></activity>
        <activity android:name=".models.BasicAppCompatActivity" />
        <activity android:name=".models.menus.BasicDrawerMenu" />
        <activity
            android:name=".controllers.DiceMenu"
            android:label="@string/title_activity_dice_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.CharacteristicsMenu"
            android:label="@string/title_activity_characteristics_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.main.MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>